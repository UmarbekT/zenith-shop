const KEY={users:'zenith_users',user:'zenith_current_user',cart:'zenith_cart',orders:'zenith_orders'};const read=(k,d)=>{try{return JSON.parse(localStorage.getItem(k))??d}catch(e){return d}};const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));const money=n=>new Intl.NumberFormat('uz-UZ').format(n)+" so'm";let cart=read(KEY.cart,[]);
function badge(){document.querySelectorAll('#cartCount').forEach(e=>e.textContent=cart.reduce((s,x)=>s+x.qty,0))}function add(id){let x=cart.find(i=>i.id===id);x?x.qty++:cart.push({id,qty:1});write(KEY.cart,cart);badge();renderCart()}function change(id,d){let x=cart.find(i=>i.id===id);if(!x)return;x.qty+=d;cart=cart.filter(i=>i.qty>0);write(KEY.cart,cart);badge();renderCart()}
function card(p){return `<article class="card"><img src="${p.image}" alt="${p.name}"><div class="card-body"><small>${p.category}</small><h3>${p.name}</h3><p>${p.description}</p><b class="price">${money(p.price)}</b><div class="card-actions"><button onclick="add(${p.id})">Savatga</button><button onclick="location.href='product.html?id=${p.id}'">Batafsil</button></div></div></article>`}function renderProducts(list=PRODUCTS){let r=document.querySelector('#productsGrid')||document.querySelector('#featuredProducts');if(r)r.innerHTML=list.map(card).join('')}
function renderCart(){let r=document.querySelector('#cartItems');if(!r)return;let sum=0;if(!cart.length){r.innerHTML='<p class="muted">Savat hozircha bo‘sh.</p>'}else{r.innerHTML=cart.map(x=>{let p=PRODUCTS.find(y=>y.id===x.id);sum+=p.price*x.qty;return `<div class="cart-item"><img src="${p.image}" alt="${p.name}"><div><b>${p.name}</b><p class="price">${money(p.price*x.qty)}</p><div class="qty"><button onclick="change(${p.id},-1)">−</button> ${x.qty} <button onclick="change(${p.id},1)">+</button></div></div></div>`}).join('')}let s=document.querySelector('#subtotal'),t=document.querySelector('#total');if(s)s.textContent=money(sum);if(t)t.textContent=money(sum?sum+150000:0)}
function authNav(){let u=read(KEY.user,null),a=document.querySelector('#authLink');if(a){a.textContent=u?'Profil':'Kirish';a.href=u?'profile.html':'login.html'}}function productPage(){let r=document.querySelector('#productDetail');if(!r)return;let p=PRODUCTS.find(x=>x.id==new URLSearchParams(location.search).get('id'))||PRODUCTS[0];r.innerHTML=`<div class="product-detail"><img src="${p.image}" alt="${p.name}"><div><small>${p.category}</small><h1>${p.name}</h1><p class="muted">${p.description}</p><h2 class="price">${money(p.price)}</h2><button class="btn" onclick="add(${p.id})">Savatga qo‘shish</button></div></div>`}
function users(){let u=read(KEY.user,null);let n=document.querySelector('#profileName'),e=document.querySelector('#profileEmail');if(n&&!u){location.href='login.html';return}if(n){n.textContent=u.name;e.textContent=u.email}document.querySelector('#logoutBtn')?.addEventListener('click',()=>{localStorage.removeItem(KEY.user);location.href='index.html'})}
function forms(){document.querySelector('#loginForm')?.addEventListener('submit',e=>{e.preventDefault();let u=read(KEY.users,[]).find(x=>x.email===loginEmail.value&&x.password===loginPassword.value);if(!u)return loginMessage.textContent='Email yoki parol noto‘g‘ri';write(KEY.user,u);location.href='profile.html'});document.querySelector('#registerForm')?.addEventListener('submit',e=>{e.preventDefault();let u={name:registerName.value,email:registerEmail.value,password:registerPassword.value},all=read(KEY.users,[]);all.push(u);write(KEY.users,all);write(KEY.user,u);location.href='profile.html'})}
document.addEventListener('DOMContentLoaded',()=>{let m=document.querySelector('#menuToggle'),n=document.querySelector('#mainNav');m?.addEventListener('click',()=>n.classList.toggle('open'));badge();authNav();let page=document.body.dataset.page;if(page==='home'){renderProducts(PRODUCTS.slice(0,4));document.querySelector('#homeSearch')?.addEventListener('input',e=>renderProducts(PRODUCTS.filter(p=>p.name.toLowerCase().includes(e.target.value.toLowerCase()))))}if(page==='products'){let f=document.querySelector('#categoryFilters');f.innerHTML=['All',...new Set(PRODUCTS.map(p=>p.category))].map(c=>`<button class="filter-pill" data-c="${c}">${c}</button>`).join('');renderProducts();document.querySelector('#shopSearch')?.addEventListener('input',e=>renderProducts(PRODUCTS.filter(p=>p.name.toLowerCase().includes(e.target.value.toLowerCase()))));f.addEventListener('click',e=>{if(e.target.dataset.c)renderProducts(e.target.dataset.c==='All'?PRODUCTS:PRODUCTS.filter(p=>p.category===e.target.dataset.c))})}if(page==='product')productPage();if(page==='cart'){renderCart();document.querySelector('#checkoutBtn')?.addEventListener('click',()=>{if(!read(KEY.user,null))return location.href='login.html';if(!cart.length)return alert('Savat bo‘sh');let o=read(KEY.orders,[]);o.push({user:read(KEY.user).email,date:new Date().toLocaleString(),items:cart,total:document.querySelector('#total').textContent});write(KEY.orders,o);cart=[];write(KEY.cart,cart);renderCart();badge();alert('Buyurtma qabul qilindi')})}if(page==='profile')users();forms()});