const KEY={users:'zenith_users',user:'zenith_current_user',cart:'zenith_cart',orders:'zenith_orders'};
const read=(key,fallback)=>{try{return JSON.parse(localStorage.getItem(key))??fallback}catch{return fallback}};
const write=(key,value)=>localStorage.setItem(key,JSON.stringify(value));
const money=value=>new Intl.NumberFormat('uz-UZ').format(value)+" so'm";
let cart=read(KEY.cart,[]);

function cartCount(){return cart.reduce((total,item)=>total+item.qty,0)}
function ensureFloatingCart(){if(document.querySelector('.floating-cart'))return;const link=document.createElement('a');link.href='cart.html';link.className='floating-cart';link.setAttribute('aria-label','Savatni ochish');link.innerHTML='🛒<span class="cart-number">0</span>';document.body.append(link)}
function updateBadges(){document.querySelectorAll('#cartCount').forEach(el=>el.textContent=cartCount());const badge=document.querySelector('.cart-number');if(badge)badge.textContent=cartCount()}
function animateCart(){const cartButton=document.querySelector('.floating-cart');if(!cartButton)return;cartButton.classList.remove('bump');void cartButton.offsetWidth;cartButton.classList.add('bump')}
function addToCart(id){const item=cart.find(entry=>entry.id===Number(id));if(item)item.qty+=1;else cart.push({id:Number(id),qty:1});write(KEY.cart,cart);updateBadges();renderCart();animateCart()}
function changeQuantity(id,delta){const item=cart.find(entry=>entry.id===Number(id));if(!item)return;item.qty+=delta;cart=cart.filter(entry=>entry.qty>0);write(KEY.cart,cart);updateBadges();renderCart()}
function productCard(product){return `<article class="card"><img src="${product.image}" alt="${product.name}"><div class="card-body"><small>${product.category}</small><h3>${product.name}</h3><p>${product.description}</p><strong class="price">${money(product.price)}</strong><div class="card-actions"><button type="button" onclick="addToCart(${product.id})">Savatga</button><a class="card-button" href="product.html?id=${product.id}">Batafsil</a></div></div></article>`}
function renderProducts(list=PRODUCTS){const root=document.querySelector('#productsGrid')||document.querySelector('#featuredProducts');if(root)root.innerHTML=list.map(productCard).join('')||'<p class="muted">Mahsulot topilmadi.</p>'}
function renderCart(){const root=document.querySelector('#cartItems');if(!root)return;let total=0;if(!cart.length)root.innerHTML='<p class="muted empty-state">Savat hozircha bo‘sh.</p>';else root.innerHTML=cart.map(item=>{const product=PRODUCTS.find(p=>p.id===item.id);if(!product)return '';total+=product.price*item.qty;return `<div class="cart-item"><img src="${product.image}" alt="${product.name}"><div class="cart-info"><b>${product.name}</b><p class="price">${money(product.price*item.qty)}</p><div class="qty"><button type="button" onclick="changeQuantity(${product.id},-1)">−</button><span>${item.qty}</span><button type="button" onclick="changeQuantity(${product.id},1)">+</button></div></div></div>`}).join('');const subtotal=document.querySelector('#subtotal');const grandTotal=document.querySelector('#total');if(subtotal)subtotal.textContent=money(total);if(grandTotal)grandTotal.textContent=money(total?total+150000:0)}
function updateAuthLink(){const user=read(KEY.user,null);const link=document.querySelector('#authLink');if(link){link.textContent=user?'Profil':'Kirish';link.href=user?'profile.html':'login.html'}}
function initMenu(){const button=document.querySelector('#menuToggle'),nav=document.querySelector('#mainNav');if(!button||!nav)return;button.addEventListener('click',()=>{const open=nav.classList.toggle('open');button.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>nav.classList.remove('open')))}
function initProductPage(){const root=document.querySelector('#productDetail');if(!root)return;const id=new URLSearchParams(location.search).get('id');const product=PRODUCTS.find(item=>item.id===Number(id))||PRODUCTS[0];root.innerHTML=`<div class="product-detail"><img src="${product.image}" alt="${product.name}"><div><small>${product.category}</small><h1>${product.name}</h1><p class="muted">${product.description}</p><h2 class="price">${money(product.price)}</h2><button class="btn" type="button" onclick="addToCart(${product.id})">Savatga qo‘shish</button></div></div>`}
function initAuthForms(){document.querySelector('#loginForm')?.addEventListener('submit',event=>{event.preventDefault();const email=document.querySelector('#loginEmail').value.trim().toLowerCase();const password=document.querySelector('#loginPassword').value;const user=read(KEY.users,[]).find(item=>item.email.toLowerCase()===email&&item.password===password);const message=document.querySelector('#loginMessage');if(!user){message.textContent='Email yoki parol noto‘g‘ri';return}write(KEY.user,user);location.href='profile.html'});document.querySelector('#registerForm')?.addEventListener('submit',event=>{event.preventDefault();const user={name:document.querySelector('#registerName').value.trim(),email:document.querySelector('#registerEmail').value.trim().toLowerCase(),password:document.querySelector('#registerPassword').value};const users=read(KEY.users,[]);const message=document.querySelector('#registerMessage');if(users.some(item=>item.email===user.email)){message.textContent='Bu email avval ro‘yxatdan o‘tgan';return}users.push(user);write(KEY.users,users);write(KEY.user,user);location.href='profile.html'})}
function initProfile(){const user=read(KEY.user,null);const name=document.querySelector('#profileName');if(name&&!user){location.href='login.html';return}if(user){name.textContent=user.name;const email=document.querySelector('#profileEmail');if(email)email.textContent=user.email}document.querySelector('#logoutBtn')?.addEventListener('click',()=>{localStorage.removeItem(KEY.user);location.href='index.html'})}
function initOrders(){const root=document.querySelector('#ordersList');if(!root)return;const user=read(KEY.user,null);if(!user){root.innerHTML='<div class="orders-card"><p>Buyurtmalarni ko‘rish uchun avval kiring.</p><a class="btn" href="login.html">Kirish</a></div>';return}const orders=read(KEY.orders,[]).filter(order=>order.user===user.email);if(!orders.length){root.innerHTML='<div class="orders-card"><p>Hozircha buyurtmalar yo‘q.</p></div>';return}root.innerHTML=orders.map(order=>`<article class="orders-card"><h3>Buyurtma #${order.id||''}</h3><p>${order.date||''}</p><p><b>${order.total}</b></p></article>`).join('')}
function init(){ensureFloatingCart();initMenu();updateBadges();updateAuthLink();initProductPage();initAuthForms();initProfile();initOrders();const page=document.body.dataset.page;if(page==='home'){renderProducts(PRODUCTS.slice(0,4));document.querySelector('#homeSearch')?.addEventListener('input',event=>{const q=event.target.value.toLowerCase();renderProducts(PRODUCTS.filter(p=>(p.name+' '+p.category).toLowerCase().includes(q)))})}if(page==='products'){const filters=document.querySelector('#categoryFilters');if(filters){filters.innerHTML=['All',...new Set(PRODUCTS.map(p=>p.category))].map(category=>`<button class="filter-pill" data-category="${category}">${category}</button>`).join('');filters.addEventListener('click',event=>{const category=event.target.dataset.category;if(category)renderProducts(category==='All'?PRODUCTS:PRODUCTS.filter(p=>p.category===category))}}renderProducts();document.querySelector('#shopSearch')?.addEventListener('input',event=>{const q=event.target.value.toLowerCase();renderProducts(PRODUCTS.filter(p=>(p.name+' '+p.category).toLowerCase().includes(q)))})}if(page==='cart'){renderCart();document.querySelector('#checkoutBtn')?.addEventListener('click',()=>{const user=read(KEY.user,null);if(!user)return location.href='login.html';if(!cart.length)return alert('Savat bo‘sh');const orders=read(KEY.orders,[]);orders.push({id:Date.now(),user:user.email,date:new Date().toLocaleString(),items:cart,total:document.querySelector('#total').textContent});write(KEY.orders,orders);cart=[];write(KEY.cart,cart);updateBadges();renderCart();alert('Buyurtma qabul qilindi');location.href='orders.html'})}}
document.addEventListener('DOMContentLoaded',init);